@echo off

cd freerdp

cmake.exe . -DOPENSSL_USE_STATIC_LIBS=TRUE -DOPENSSL_MSVC_STATIC_RT=TRUE -DMONOLITHIC_BUILD=ON -DBUILD_SHARED_LIBS=OFF -G "Visual Studio 14" -T v140_xp -DMSVC_RUNTIME=static -DWITH_SSE2=ON

cd ..
